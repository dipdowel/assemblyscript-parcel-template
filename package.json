{
  "name": "assemblyscript-parcel-template",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "clean:dist": "rimraf dist/*",
    "clean:build": "rimraf build/*",
    "start": "yarn clean:dist && parcel src/index.html",
    "build": "yarn clean:dist && parcel build src/index.html"
  },
  "devDependencies": {
    "parcel": "^2.9.1",
    "parcel-transformer-assemblyscript-codument": "^0.1.1-alpha",
    "parcel-config-assemblyscript-codument": "0.1.4",
    "rimraf": "^5.0.0",
    "prettier": "^2.8.8"
  }

}
